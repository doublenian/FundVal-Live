name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: macOS
          - os: windows-latest
            platform: Windows
          - os: ubuntu-latest
            platform: Linux

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install uv (Unix)
        if: runner.os != 'Windows'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install uv (Windows)
        if: runner.os == 'Windows'
        run: irm https://astral.sh/uv/install.ps1 | iex
        shell: powershell

      - name: Install Python dependencies
        run: |
          uv pip install --system -r backend/requirements.txt
          uv pip install --system pyinstaller

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Build backend with PyInstaller
        run: uv run pyinstaller backend.spec --clean

      - name: Install Electron dependencies
        run: npm ci

      - name: Build Electron app (macOS arm64)
        if: runner.os == 'macOS'
        run: npm run dist -- --arm64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (macOS x64)
        if: runner.os == 'macOS'
        run: npm run dist -- --x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Windows)
        if: runner.os == 'Windows'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Linux)
        if: runner.os == 'Linux'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: release/*.dmg
          if-no-files-found: error

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: release/*.exe

      - name: Upload artifacts (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: |
            release/*.AppImage
            release/*.deb

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          body: |
            ## 安装包下载

            ### macOS
            - **Apple Silicon (M1/M2/M3)**: `FundVal-Live-*-arm64.dmg`
            - **Intel**: `FundVal-Live-*-x64.dmg`

            ### Windows
            - **64位**: `FundVal-Live-Setup-*.exe`

            ### Linux
            - **AppImage**: `FundVal-Live-*.AppImage`
            - **Debian/Ubuntu**: `fundval-live_*_amd64.deb`

            ## 本次更新 (v1.2.6)

            ### 🎨 新功能
            - **夜间主题**: 全新的暗色模式，支持一键切换，保护眼睛
              - 自动跟随系统主题
              - 平滑过渡动画
              - 图表组件完美适配暗色模式

            ### 🐛 Bug修复
            - **数据库连接池优化**: 修复了73处不当的连接关闭操作，提升系统稳定性
            - **AI分析数据一致性**: 修复AI分析时技术指标与前端显示不一致的问题
            - **图表显示**: 修复暗色模式下图表颜色对比度不足的问题

            ### ⚡ 性能优化
            - 优化数据库连接池，减少连接创建开销
            - 优化主题切换性能，减少不必要的localStorage写入
            - 改进图表渲染性能

            ### 🔧 技术改进
            - 重构数据库连接管理，使用thread-local连接池
            - 统一AI分析和前端显示的数据源
            - 清理重复的CSS类名，减小bundle体积

            ---

            
            ## 使用说明

            1. 下载对应平台的安装包
            2. 双击安装
            3. 启动应用即可使用

            数据存储位置：
            - 数据库：`~/.fundval-live/data/fund.db`
            - 日志：`~/.fundval-live/logs/`

            ## 注意事项

            - **macOS**：
              - 首次打开可能提示"无法验证开发者"，请在系统偏好设置 → 安全性与隐私中允许
              - 如提示"应用已损坏"，在终端执行：
                ```bash
                sudo xattr -dr com.apple.quarantine "/Applications/FundVal Live.app"
                ```
            - **Windows**：可能提示 SmartScreen，点击"更多信息"→"仍要运行"
            - **Linux**：AppImage 需要添加执行权限：`chmod +x FundVal-Live-*.AppImage`

            ## 开源协议

            本项目采用 AGPL-3.0 协议开源。如果你使用本项目代码提供网络服务，必须开源你的修改。
            详见：https://github.com/Ye-Yu-Mo/FundVal-Live/blob/main/LICENSE
          files: |
            macos-build/*
            windows-build/*
            linux-build/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
